Хорошо, что я давно отучил себе писать код из примера первого занятия. Наверно еще в школе(исключения были, конечно же).
Я наверно могу себе позволить такой код на проде, когда происходит дикая потеря данных и срочно нужно фиксить.
Благо в важных(да и не очень) важных частях рабочих проектов подобного не встречал, да и не ревью я бы такое не пропустил.

Процедурный стиль с использованием глобальных переменных для себя убрал подальшу в ящик и стараюсь забыть про него.
Либо функция явно возвращает значение, либо не возвращает. Конечно в С++ есть Сишное наследие, где мы частенько заполняем
 память, переданную в процедуру, но это явно обозначено в документации. В условиях Питонячего мира на мой взгляд либо 
 ограничиваем область видимости переменных классом(то есть от глоабльного состояния системы мы получаем внутреннее состояние объекта,
 которое контролировать легче), либо пишем чистыми функциями.